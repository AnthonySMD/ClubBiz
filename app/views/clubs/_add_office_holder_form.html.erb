<%= form_for @club do |f| %>
  <% if @club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club.errors.count, "error") %> prohibited this club from being saved:</h2>
      <ul>
      <% @club.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<table align="center">
  <tr><div>
    <td><%= f.label :name %></td>
    <td><%= f.text_field :name %></td>
  </div></tr>
  <tr><div>
    <td><%= f.label :email %></td>
    <td><%= f.text_field :email %></td>
  </div></tr>
  <tr><div>
    <td><%= f.label :office_holder %></td>
    <td><%= f.text_field :office_holder %></td>
  </div></tr>
  </table>
    <br/>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
